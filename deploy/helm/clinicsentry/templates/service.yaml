apiVersion: v1
kind: Service
metadata:
  name: {{ include "clinicsentry.fullname" . }}
  labels: {{- include "clinicsentry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "clinicsentry.selectorLabels" . | nindent 4 }}
